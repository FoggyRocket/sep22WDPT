<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>

    <p class="card-text">{{description}}</p>

    {{!-- if --}}
    {{#if isComplete}}
      <p>Terminada</p>
    {{else}}
      <p>sin comenzar</p>
    {{/if}}
    {{!-- ejemplo with 
      {{#with subtareas}}
        <p>{{name}}: </p>
      {{/with}}
    --}}
    <a href="#" class="card-link">editar link</a>
    <a href="#" class="card-link">eliminar link</a>
  </div>
</div>